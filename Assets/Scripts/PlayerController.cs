using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    public float maxSpeed = MainManager.Instance.maxSpeed;
    public float maxMass = MainManager.Instance.maxMass;
    public float maxDive = MainManager.Instance.maxDive;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void HookThrowing()
    {
        //TODO
    }
}
